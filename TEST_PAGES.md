# 페이지별 테스트 가이드

## 브라우저에서 확인할 사항

### 1. 개발자 도구 콘솔 확인
`F12` 또는 `Cmd+Option+I` 로 열고:
- 빨간색 에러 메시지가 있는지 확인
- React 에러가 있는지 확인

### 2. 네트워크 탭 확인
- `/api/spots` 요청이 성공했는지 (200 OK)
- 응답이 `[]`인지 확인

### 3. Elements 탭 확인
점주 관리 페이지에서:
- `.owner-dashboard` 요소가 있는지
- `.owner-dashboard-header` 요소가 보이는지
- 텍스트 "아직 만든 스팟이 없습니다"가 DOM에 있는지

## 페이지별 테스트 체크리스트

### ✅ 역할 선택 화면
- [ ] "Tokamon" 제목이 보임
- [ ] "고객으로 사용" 버튼 동작
- [ ] "점주로 사용" 버튼 동작

### ✅ 고객 - 지도 탭
- [ ] 지도가 로드됨 (서울 중심)
- [ ] "지도에서 스팟을 선택하세요 (0개 활성 스팟)" 메시지 표시
- [ ] 지갑 연결 버튼 클릭 시 MetaMask 팝업

### ✅ 고객 - 스팟 목록 탭
- [ ] "등록된 스팟이 없습니다" 메시지 표시
- [ ] 필터 버튼 (활성 스팟 / 전체) 동작

### ✅ 고객 - 내 기록 탭
- [ ] 지갑 미연결 시: "클레임 기록을 보려면 지갑을 연결해주세요"
- [ ] 지갑 연결 시: "아직 클레임 기록이 없습니다"

### ✅ 점주 - 내 스팟 관리 탭 ⚠️ 문제 발생
**지갑 미연결:**
- [ ] "지갑을 연결하면 내 스팟을 관리할 수 있습니다" 메시지
- [ ] "지갑 연결" 버튼 보임

**지갑 연결 후:**
- [ ] "내 스팟 (0개)" 헤더 보임
- [ ] "아직 만든 스팟이 없습니다" 메시지 보임

### ✅ 점주 - 스팟 만들기 탭
- [ ] 지갑 미연결 시: "스팟을 만들려면 지갑을 연결해주세요"
- [ ] 지갑 연결 후: "지도를 탭하여 스팟을 만들 위치를 선택하세요"
- [ ] 지도 클릭 시 생성 폼 팝업

## 예상되는 화면 텍스트

### 점주 - 내 스팟 관리 (지갑 연결 후)
```
내 스팟 (0개)
아직 만든 스팟이 없습니다
```

이 텍스트가 **반드시** 보여야 합니다.

## 문제 해결 방법

### 화면이 보이지 않는 경우

1. **CSS 로드 확인**
   - 브라우저 개발자 도구 → Sources → `style.css` 파일 있는지 확인

2. **React 렌더링 확인**
   - 콘솔에 에러가 있는지 확인
   - Elements 탭에서 `.owner-dashboard` 요소가 있는지 확인

3. **spots 상태 확인**
   - React DevTools 설치 후 Components 탭에서 `App` 컴포넌트의 `spots` state 확인
   - `undefined`가 아니라 `[]` 빈 배열이어야 함

4. **wallet 상태 확인**
   - React DevTools에서 `wallet` state가 지갑 주소로 설정되었는지 확인

## 브라우저 콘솔에서 직접 테스트

```javascript
// 1. API 직접 호출
fetch('http://localhost:3001/api/spots').then(r => r.json()).then(console.log)

// 2. 지갑 연결 상태 확인
console.log(window.ethereum)
```
